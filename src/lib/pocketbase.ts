import PocketBase from 'pocketbase';

// This is the default URL for a local PocketBase instance.
// Make sure to replace it with your actual server URL.
export const pb = new PocketBase('http://127.0.0.1:8090');